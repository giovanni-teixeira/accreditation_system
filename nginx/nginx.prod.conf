events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Otimizações
    sendfile        on;
    keepalive_timeout  65;

    # Configurações globais de proxy
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;

    # Redirecionamento Global HTTP -> HTTPS baseada no feedback do usuário.
    server {
        listen 80;
        server_name credenciamento.altacafe.com.br;
        return 301 https://$host$request_uri;
    }

    # Bloco para porta 443 (HTTPS - Produção) baseada no feedback do usuário.
    server {
        listen 443 ssl;
        server_name credenciamento.altacafe.com.br; 

        # Certificados SSL Reais (Montados via Volume Docker) baseada no feedback do usuário.
        ssl_certificate     /etc/letsencrypt/live/credenciamento.altacafe.com.br/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/credenciamento.altacafe.com.br/privkey.pem;

        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;
        ssl_session_cache   shared:SSL:10m;
        ssl_session_timeout 10m;

        # Frontend Principal baseada no feedback do usuário.
        location / {
            proxy_pass http://frontend:3000;
        }

        # Admin (Alta Café) baseada no feedback do usuário.
        location /admin {
            proxy_pass http://alta_cafe:3000;
        }

        # Scanner baseada no feedback do usuário.
        location /scanner {
            proxy_pass http://scanner:3000;
        }

        # API Backend baseada no feedback do usuário.
        location /api/ {
            proxy_pass http://backend:3001/;
        }
    }
}
